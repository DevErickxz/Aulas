conversão de dados ou coerção de dados.
pra que serve?
para converter um tipo de dado para outro
um exemplo real:

a = 1
print(type(bool(a)))

pra eu transformar um tipo em outro é so usar o nome dela exemplo

bool
a = 1
print(type(bool(a)))

bool -> booleano -> True ou False

